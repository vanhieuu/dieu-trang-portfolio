import React from 'react';

type Props = {};

const About = (props: Props) => {
  return (
    <div className='h-screen flex flex-col h-screen text-center md:text-left md:flex-row max-w-7xl px-10'></div>
  );
};

export default About;
